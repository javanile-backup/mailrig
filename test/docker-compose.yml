version: '2'

services:

  mailman: &mailman
    build: ../
    volumes: ['./tasks:/tasks']

  move1: { <<: *mailman, command: ["--task", "tasks/move1"] }
  move2: { <<: *mailman, command: ["--task", "tasks/move2"] }
